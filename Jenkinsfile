pipeline {
    agent any
	stages{
	    stage('Init'){
		    steps {
			    echo "Testing Now.."
			}
		}
		
		stage('Build'){
		    steps {
			    echo "Building Now.."
			}
		}
		
		stage('Deploy'){
		    steps {
			    echo "Code Deployed.."
			}
		}
	}
}
